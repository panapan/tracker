<% provide(:title, '') %>
<h1>Проверить статус посылки</h1>
<%= render 'pages/add' %>

<h1>Ваши посылки</h1>
<% if @parcels.nil? || @parcels.count == 0  %>
  <p>Пользователь еще не добавил ни одной посылки для отслеживания</p>
<% else %>
  <%= render @parcels %>
  <%= will_paginate @parcels, params: {controller: 'pages', action: 'home'} %>
<% end %>
